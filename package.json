{
  "name": "n8n-workflow-analyzer",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w ui\"",
    "build": "npm run build -w packages/types && npm run build -w packages/core && npm run build -w packages/server && npm run build -w packages/ui",
    "test": "npm run test -w packages/core",
    "lint": "npm run lint -w packages/core && npm run lint -w packages/server && npm run lint -w packages/ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
